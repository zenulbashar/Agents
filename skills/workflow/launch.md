---
description: "Marketing launch: GTM execution across channels - publishing externally is a HUMAN GATE (Foundry stage 10)."
argument-hint: "<product/feature to launch>"
allowed-tools: Task, Read, Grep, Glob
---
Launch: $ARGUMENTS

1. Use the `marketing-director` subagent to plan GTM across `seo`, `content`, `social-media`, and `growth`; `web` builds the landing page and `graphic-design` the assets.
2. BRIGHT LINE: any external publishing (social posts, blog go-live, store listing) is a human gate. Produce drafts and STOP for operator approval before anything publishes.

Then run `/foundry:workflow:reflect`.
