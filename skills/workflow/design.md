---
description: "Design: user flows, wireframes, and an implementable UI spec (Foundry stage 3)."
argument-hint: "<feature needing UX/UI>"
allowed-tools: Task, Read, Grep, Glob
---
Design the experience for: $ARGUMENTS

1. Use the `ux` subagent for flows, information architecture, and wireframes with accessibility designed in.
2. Use the `ui` subagent for design tokens and a spec the Frontend can implement 1:1.

Route to `product-owner` for acceptance. End by recommending `/foundry:workflow:build`.
