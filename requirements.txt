# Foundry orchestrator + tooling (Phase 1 skeleton).
# Pin/upgrade as the platform matures; these are the lower bounds it targets.
pyyaml>=6.0
langgraph>=0.2
langchain-core>=0.3
pydantic>=2.7
pydantic-ai>=0.0.14
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
redis>=5.0
psycopg[binary]>=3.1
qdrant-client>=1.9
ollama>=0.3
mcp>=1.0
python-dotenv>=1.0
rich>=13.7
